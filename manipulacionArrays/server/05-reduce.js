const totals = [1,2,3,4,5];

const rta = totals.reduce((sum, element) => sum + element, 0); // Primer valor acumulador, segundo valor elemento y ultimo parametro, valor inicial
console.log('rta');
console.log(rta);